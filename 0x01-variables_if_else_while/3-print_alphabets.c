#include <stdio.h>
/**
 * main - print in caps and lower case
 * Return: 0 (working)
 */
int main(void)
{
	char Lower = 'a';
	char Upper = 'A';

	while (lower < = 'z')
	{
		putchar(lower);
		Lower++;
	}
	while (upper < = 'z')
	{
		putchar(upper);
		Upper++;
	}
	putchar('\n');
	return (0);
}
